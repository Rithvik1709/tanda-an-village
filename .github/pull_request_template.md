## What changed

<!-- A sentence or two, e.g. "Adds cotton as a kharif crop with four growth stages." -->

## How I tested it

- [ ] `npm run typecheck`
- [ ] `npm test`
- [ ] `npm run build`
- [ ] Ran the play-through for this area (e.g. `scripts/m3.js`) and looked at the screenshots
- [ ] Game rules changed only in `src/shared/rules.ts` (with a test); save format migrations added if needed

## Screenshots

<!-- Before / after for anything visual. -->
